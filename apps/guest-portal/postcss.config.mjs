// Tailwind v4 is a PostCSS plugin rather than a config file; the theme lives in
// @ranza/ui/globals.css, which this application's own stylesheet imports.
export default {
  plugins: ["@tailwindcss/postcss"],
};
